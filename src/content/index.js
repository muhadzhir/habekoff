import { cards as contentCards } from './cards'
import { littleCards as contentLittleCards } from './littleCards'
export default {
  data() {
    return {
      contentCards,
      contentLittleCards
    }
  }
}
